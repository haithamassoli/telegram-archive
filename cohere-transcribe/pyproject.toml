[build-system]
requires = ["uv_build>=0.11.28,<0.12"]
build-backend = "uv_build"

[project]
name = "cohere-transcribe"
version = "0.1.4"
description = "Unofficial high-throughput batch inference for Cohere Arabic/English ASR"
readme = "README.md"
license = "Apache-2.0 AND CC-BY-NC-4.0 AND MIT"
license-files = [
  "LICENSE",
  "NOTICE",
  "THIRD_PARTY_NOTICES.md",
  "src/cohere_transcribe/alignment/LICENSE",
  "src/cohere_transcribe/alignment/UPSTREAM.md",
  "src/cohere_transcribe/vad/LICENSE.*",
  "src/cohere_transcribe/vad/README.md",
]
requires-python = ">=3.10,<3.14"
authors = [{ name = "Ali Osm" }]
keywords = ["arabic", "asr", "batch-inference", "cohere", "speech-recognition"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Multimedia :: Sound/Audio :: Speech",
  "Typing :: Typed",
]
dependencies = [
  "huggingface-hub",
  "librosa>=0.11.0",
  "numpy",
  "packaging",
  "torch==2.11.0",
  "torchcodec==0.14.0",
  "tqdm",
  "transformers[sentencepiece]==5.13.1",
]

[project.optional-dependencies]
adapters = ["peft>=0.19.1"]
auditok = ["auditok>=0.4.2,<0.5"]
onnx = [
  "onnxruntime>=1.16.1,<1.24; python_version < '3.11'",
  "onnxruntime>=1.16.1; python_version >= '3.11'",
]
quantized = ["accelerate>=1.13.0", "bitsandbytes>=0.49.2"]
word = ["torchaudio==2.11.0", "uroman==1.3.1.1"]

[project.urls]
Documentation = "https://github.com/AliOsm/cohere-transcribe#readme"
Issues = "https://github.com/AliOsm/cohere-transcribe/issues"
Repository = "https://github.com/AliOsm/cohere-transcribe"

[project.scripts]
cohere-transcribe = "cohere_transcribe.cli:cli"
cohere-transcribe-doctor = "cohere_transcribe.doctor:main"

[dependency-groups]
dev = [
  "mypy>=1.19,<2",
  "pytest>=8.4",
  "ruff==0.15.21",
  "silero-vad==6.2.1",
  "tomli>=2; python_version < '3.11'",
]

[tool.uv.build-backend]
module-name = "cohere_transcribe"
source-include = [
  "CHANGELOG.md",
  "docs/**",
  "reports/**",
  "scripts/**",
  "tests/**",
]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
filterwarnings = [
  "ignore:`torch\\.jit\\.load` is deprecated.*:DeprecationWarning",
]

[tool.ruff]
line-length = 88
target-version = "py310"
extend-exclude = [
  "src/cohere_transcribe/alignment/alignment_utils.py",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = [
  "B009",   # Explicit getattr documents optional third-party attributes.
  "E501",
  "RUF046", # Retain casts at external-library and NumPy type boundaries.
]

[tool.ruff.format]
docstring-code-format = true
